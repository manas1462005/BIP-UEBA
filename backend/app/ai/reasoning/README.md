# Reasoning Submodule

## Responsibilities
- Combine behavioral anomaly scores with environmental context (MFA status, VPN, device fingerprint).
- Synthesize multi-factor risk scores (`behaviour_score`, `trust_score`, `context_score`, `threat_score`).
- Correlate events across multiple entities to detect lateral movement.
